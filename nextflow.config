params {
    config = "default"
    resources = "default"
    container = "np-core/pathogen:latest"

    kraken_threads = 4
    kraken_memory = "8G"
    
}

includeConfig("configs/${params.config}.config")